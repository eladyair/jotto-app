import { GUESS_WORD } from '../types';

/**
 * @function guessWord
 * @returns {object} - Action object with type 'GUESS_WORD'
 */
export const guessWord = guesedWord => {
    return (dispatch, getState) => {};
};
